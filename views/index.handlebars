
<center><h1>Eat Da Burger!</h1></center>

<center><img src="assets/img/burger.jpg" alt=""></center>

<H3>Devoured</H3>
<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger-block devou=true}}
    {{/if}}
  {{/each}}
</ul>

<h3>Devour</h3>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger-block devou=false}}
    {{/unless}}
  {{/each}}
</ul>

<h3>Add a Burger</h3>
<form class="create-form" integrity="">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>  

  <button type="submit" data-newBurger="burger_name">Add Burger</button>
</form>
